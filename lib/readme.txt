Step:
1) Download latest SDL version, link here [https://github.com/libsdl-org/SDL/releases/latest].
2) Download latest SDL_Image version, link here [https://github.com/libsdl-org/SDL_image/releases/latest].
3) Move all the file from the directory lib in SDL_Image in the lib directory in SDL.
4) Copy the file inside lib [SDL's one] here.